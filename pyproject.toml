[project]
name = "qwazon-llm"
version = "0.3.0"
description = "Qwazon — złoty środek między wydajnością a inteligencją. LLM na ziemniaka, mądry jak duży."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Qwazon Team"}]
keywords = ["llm", "ai", "code", "polish", "moe", "efficient", "fastapi", "lora", "dpo"]
dependencies = [
  "torch>=2.1.0",
  "transformers>=4.38.0",
  "tokenizers>=0.15.0",
  "numpy>=1.24.0",
  "tqdm",
  "pyyaml",
  "psutil",
  "fastapi",
  "uvicorn",
  "gradio>=4.0",
]
[project.optional-dependencies]
train = ["datasets>=2.16.0", "accelerate>=0.27.0", "safetensors>=0.4.0", "bitsandbytes"]
demo = ["gradio>=4.0"]
api = ["fastapi", "uvicorn", "httpx"]
lora = ["bitsandbytes"]
gguf = ["gguf>=0.9.0"]
all = ["datasets>=2.16.0", "accelerate>=0.27.0", "safetensors>=0.4.0", "gradio>=4.0", "gguf>=0.9.0", "fastapi", "uvicorn", "bitsandbytes"]

[project.scripts]
qwazon-train = "scripts.train:main"
qwazon-generate = "scripts.generate:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
