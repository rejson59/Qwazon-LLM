# Qwazon v0.3 — zależności złotego środka
# Instalacja: pip install -r requirements.txt
# Dla CPU-only ziemniaka: pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.1.0
transformers>=4.38.0
tokenizers>=0.15.0
datasets>=2.16.0
accelerate>=0.27.0
safetensors>=0.4.0
numpy>=1.24.0
tqdm
pyyaml
psutil
fastapi
uvicorn
gradio>=4.0

# Opcjonalne (przyspieszają ziemniaka)
# bitsandbytes — 8-bit AdamW, QLoRA (pip install bitsandbytes)
# flash-attn — GPU 2-3x szybciej (pip install flash-attn)
# gguf — export do llama.cpp/Ollama (pip install gguf)
# onnx — export do ONNX Runtime

# Dev:
pytest
httpx

