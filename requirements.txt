# Qwazon v0.1 — zależności złotego środka
# Instalacja: pip install -r requirements.txt
# Dla CPU-only ziemniaka: pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.1.0
transformers>=4.38.0
tokenizers>=0.15.0
datasets>=2.16.0
accelerate>=0.27.0
safetensors>=0.4.0
numpy>=1.24.0
tqdm
pyyaml
psutil

# Opcjonalne (dla treningu / kwantyzacji / demo)
# flash-attn — tylko GPU, przyspiesza 2-3x
# bitsandbytes — QLoRA / 8-bit AdamW
# gradio — demo chat
# gguf — export do llama.cpp / Ollama
# sentencepiece — własny tokenizer PL

# Dev:
pytest
